.config/karabiner/automatic_backups
.config/karabiner/automatic_backups/**

.config/.claude/settings.local.json

# Runtime log written by cmux's notification-sound script — not config, don't track
.config/cmux/notification-sound.log

# This repo's own CLAUDE.md doc lives at the source root; the leading slash
# anchors to $HOME so it is NOT deployed to ~/CLAUDE.md (but stays as repo docs).
# Anchored so it does NOT also match .config/CLAUDE.md, which IS managed.
/CLAUDE.md
