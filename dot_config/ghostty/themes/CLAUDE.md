# Ghostty Themes

Custom themes in this directory must also be copied to cmux's theme directory for cmux to use them:

```
cp <theme> ~/Library/Application\ Support/com.cmuxterm.app/themes/
```

After adding or modifying a theme, copy it to both locations.
